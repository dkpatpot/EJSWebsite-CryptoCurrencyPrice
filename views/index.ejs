<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Crypto Currency Price</title>
    <link rel="stylesheet" type="text/css" href="styles/main.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <section>
      <header class="p-3 text-bg-dark">
        <div class="container">
          <div
            class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start"
          >
            <div class="margin-nav"><h4>Crypto Currency Price</h4></div>

            <ul
              class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0"
            >
              <li>
                <a
                  href="https://github.com/dkpatpot"
                  class="nav-link px-2 text-white"
                  >Github</a
                >
              </li>
            </ul>
          </div>
        </div>
      </header>
    </section>
    <section class="table-section">
      <% if (locals.token) {%>
      <div id="right_panel">
        <div class="wrap-table">
          <table>
            <thead>
              <tr>
                <th scope="col" class="sticky-col">Token-Currency</th>
                <th scope="col">Price 24h</th>
                <th scope="col">Volume 24h</th>
                <th scope="col">Last Trade Price</th>
                <th scope="col">Date</th>
              </tr>
            </thead>

            <tbody>
              <% for (let index = 0; index < token.length;index++) {%>
              <tr>
                <td class="sticky-col" scope="row" data-label="Customer">
                  <div class="user__info">
                    <div>
                      <div class="user__name"><%= token[index].symbol%></div>
                    </div>
                  </div>
                </td>
                <% if (token[index].price_24h == 0) { %>
                <td data-label="Status">
                  <div class="user__badge user__badge_unpaid">
                    <%= token[index].price_24h %> USD
                  </div>
                </td>
                <% } else { %>
                <td data-label="Status">
                  <div class="user__badge"><%= token[index].price_24h %> USD</div>
                </td>
                <% } %>

                <td data-label="Course">
                  <span><%= token[index].volume_24h %></span>
                </td>
                <td data-label="Enrolled">
                  <%= token[index].last_trade_price %>
                </td>
                <td data-label="Date"><%= date %></td>
              </tr>
            </tbody>
            <%} %>
          </table>
        </div>
      </div>
      <%}else{ %> 
        <div class="table-section"><h4><%= error %></h4></div>
        <% } %>
    </section>
  </body>
</html>
